name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache: true

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-clean:"; echo "$unformatted"; exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: go test -race + coverage
        run: go test -race -coverprofile=coverage.out ./...

      # The browser transport (engine/wasm) is js,wasm-only, so `go test ./...`
      # above never compiles or runs it. Without this lane a bug in the wasm
      # write path — like the grip-drag array coercion that shipped silently
      # broken (#91) — passes CI green. Run the js,wasm tests under Node using
      # the toolchain's exec shim.
      - name: go test (js,wasm) under Node
        run: |
          GOROOT=$(go env GOROOT)
          PATH="$GOROOT/lib/wasm:$GOROOT/misc/wasm:$PATH" \
            GOOS=js GOARCH=wasm go test ./engine/wasm/...

      # Fuzz targets guard the untrusted-input paths (the /set wire coercer, the
      # directive + layout parsers). A short run on every push catches a newly
      # introduced panic or invariant break; deep fuzzing is a manual/scheduled
      # activity. -fuzzminimizetime keeps a failure's shrink bounded.
      - name: fuzz smoke (untrusted-input paths)
        run: |
          go test -run=xxx -fuzz=FuzzCoerceWire -fuzztime=20s ./engine/
          go test -run=xxx -fuzz=FuzzParseLayout -fuzztime=20s ./internal/analyze/
          go test -run=xxx -fuzz=FuzzDirectives -fuzztime=20s ./internal/analyze/

      - name: coverage floor (library packages, examples excluded)
        run: |
          go test -coverprofile=cover.lib.out $(go list ./... | grep -v '/examples/') >/dev/null
          total=$(go tool cover -func=cover.lib.out | awk '/^total:/ {print substr($3, 1, length($3)-1)}')
          echo "library coverage: ${total}%"
          awk -v t="$total" 'BEGIN { if (t+0 < 75.0) { print "FAIL: library coverage " t "% is below the 75% floor"; exit 1 } }'

      # golangci-lint runs in its own workflow (.github/workflows/lint.yml) so it
      # has a dedicated live badge — the honest replacement for the retired Go
      # Report Card.

  # govulncheck scans dependencies AND the standard library against the Go
  # vulnerability database (golang.org/x/vuln). Its own job so a vuln is a
  # distinct, legible CI failure rather than buried in the test lane.
  govulncheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache: true
      - name: govulncheck
        run: go run golang.org/x/vuln/cmd/govulncheck@latest ./...
